# GAM 200 : Thomas the Game Engine

Note : copy and paste dll into folder of sandboxapp.exe (temporary fix)
Note #2 : run commands below to make sure dependencies are installed
git submodule init 
git submodule update
