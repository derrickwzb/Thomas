#pragma once

namespace Thomas {

	//typedef void(*InitBehaviour) (GameObject*);
	//typedef void(*UpdateBehaviour) (GameObject*);
	//typedef void(*EndBehaviour) (GameObject*);

	//class BehaviourFCT {
	//private:
	//	InitBehaviour m_InitBehaviour;
	//	UpdateBehaviour m_UpdateBehaviour;
	//	EndBehaviour m_EndBehaviour;

	//public:
	//	EndBehaviour(const InitBehaviour& Init, const UpdateBehaviour& Update, const EndBehaviour& End) :
	//		m_InitBehaviour(Init), m_UpdateBehaviour(Update), m_EndBehaviour(End) {
	//		MyLogicSystem.AddBehaviour(this);
	//	}
	//};

}
